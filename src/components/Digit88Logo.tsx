export function Digit88Logo({ className = "" }: { className?: string }) {
  return (
    <img 
      src="/logo.png" 
      alt="Digit88 Logo" 
      className={`object-contain ${className}`}
    />
  );
}
